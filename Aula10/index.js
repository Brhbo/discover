/* TIPOS DE DADOS PRIMITIVOS: */

const nome = 'Bruno'; // Todos esses 3 tipos de string SÃO PERMITIDOS 
const nome1 = "Bruno"; // TOP DEMAIS
const nome2 = `Bruno`; // MUITO TOP
const num1 = 10; // number
const num2 = 10.52; //number
let nomeAluno; // undefined = não aponta pra local nenhum na memória
const sobrenomeAluno = null; // nulo = não aponta pra local nenhum na memória
/* Nesse caso desse NULL, é pra quando voce colocar por exemplo, um usuário
precisa escolher o fundo da pagina do facebook, dai se ele não escolhe
voce tem que colocar uma mensagem lá falando que está falando ele escolher
 */
const boolean = true; /*Esse tipo de declaração (VALOR LÓGICO) usando boolean 
você pode colocar com verdadeiro ou falso alguma função.
por exemplo um sistema que diz se o Aluno foi reprovado
ou aprovado */

console.log(typeof nome); /* Esse função é utilizada pra você saber o tipo
de variavel que você está usando */